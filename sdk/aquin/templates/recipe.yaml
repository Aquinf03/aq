# Path map for this run (prefer Aquin.define / Run in example.py).
# Runtime output lives under artifacts/ — not here.

family: tabular
method: linear
data:
  path: data.csv
  target: y
eval:
  metric: mse
  min_score: null

# --- other shapes (uncomment / swap) ---
# family: tabular
# method: ridge | logistic | forest | boosting | …
#
# family: llm
# method: lora
# model: meta-llama/Llama-3.2-1B-Instruct
# data:
#   path: data.jsonl
#   prompt: prompt
#   completion: completion
# eval:
#   metric: loss

# --- job plans (optional; or aq.plan / @schedule in SDK) ---
# plans:
#   nightly:
#     kind: cron
#     every: 60
#     run: train
#   pipe:
#     kind: pipeline
#     steps: [train, eval]

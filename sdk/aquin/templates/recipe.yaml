# Run spec. Paths are relative to this file (or absolute).
# Identity: this YAML + artifacts/ — no slot forest required.

family: tabular
method: linear
data:
  path: data.csv
  target: y
eval:
  metric: mse
  min_score: null

# --- other shapes (uncomment / swap) ---
# family: tabular
# method: ridge | logistic | forest | boosting | …
#
# family: llm
# method: lora
# model: meta-llama/Llama-3.2-1B-Instruct
# data:
#   path: data.jsonl
#   prompt: prompt
#   completion: completion
# eval:
#   metric: loss

# --- job plans (optional; state under artifacts/jobs/plans/) ---
# plans:
#   nightly:
#     kind: cron
#     every: 60
#     run: train
#   pipe:
#     kind: pipeline
#     steps: [train, eval]
